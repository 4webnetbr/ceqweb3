# Plano de Testes — Reformulação dos Tipos de Alerta de Notificações SMS (Módulo Logística)

**Projeto:** CeqWeb 3.0 · **Módulo:** Logística (`app/Controllers/Logistica`) · **Componentes envolvidos:** `NotifSmsConfig` (Controller/CRUD) · `EntLogNotifSmsConfig` (Entity) · `LogisNotifSmsConfigModel` · `LogisNotifSmsEnviadasModel` · `MyValidation` · `NotifSmsVerificar` (comando CLI) · `SmsApiConsumer` (novo) · `SmsRegraExecutor` (novo) · `ConfigDicDadosModel` · migration `2026-07-22-000001_LogisticaNotifSmsTiposApiConsulta`
**Origem:** Plano de testes definido pelo `bytest`, revisado por `byarq` (3 ajustes incorporados).
**Documento de referência:** Documento de desenvolvimento aprovado — `docs/desenvolvimento/notificacoes-sms-tipos-alerta-dev.md` (seções 4 a 12).
**Status:** aguardando nova aprovação do `byarq`.

## 0. Pré-condição bloqueante para execução

A migration `app/Database/Migrations/2026-07-22-000001_LogisticaNotifSmsTiposApiConsulta.php` **ainda não foi executada** em ambiente de dev. Ela exclui definitivamente as regras `nsc_tipo_regra = 'entrega'` já cadastradas (decisão de negócio confirmada pelo Douglas, ver seção 3, item 2 do documento de desenvolvimento) — portanto, conforme a regra do projeto de nunca rodar migration sem confirmação explícita do usuário, **este plano não pode ser executado em sua totalidade sem a confirmação prévia do Douglas para rodar a migration em dev**.

Os casos do Grupo 1, os casos do Grupo 2 que dependem do schema novo (todos, exceto os que testam apenas validação estática/pura de código sem tocar o banco) e todos os casos do Grupo 3 dependem do schema já migrado. O caso **4.9** é exceção explícita — é unitário, isolado, e **não depende da migration**: pode ser executado imediatamente. Ver detalhamento na seção "Pendências antes da execução", ao final deste documento.

## 1. Grupo 1 — Migration (schema)

| ID | Situação/Objetivo | Pré-condição | Passos | Resultado esperado |
|---|---|---|---|---|
| 1.1 | Exclusão definitiva de regras `entrega` | Ao menos 1 regra com `nsc_tipo_regra='entrega'` cadastrada em `log_notif_sms_config`, em ambiente de dev | Rodar `up()` da migration | As regras `entrega` são removidas em definitivo (`DELETE`, não soft delete); `CLI::write()` reporta **exatamente** a string `"{$qtd} regra(s) do tipo 'entrega' excluída(s) definitivamente (tipo descontinuado, confirmado pelo Douglas)."` com `{$qtd}` substituído pela contagem real de linhas excluídas (checar a string completa e exata — conforme `excluiRegrasEntrega()` na migration —, não apenas uma substring/resumo) |
| 1.2 | `ENUM` de `nsc_tipo_regra` sem `entrega` | Migration aplicada (`up()` executado) | Inspecionar via `information_schema.COLUMNS`/`SHOW COLUMNS` a definição de `nsc_tipo_regra` | `ENUM('saldo_baixo','api','consulta') NOT NULL DEFAULT 'saldo_baixo'` — `'entrega'` não consta mais na lista de valores |
| 1.3 | Colunas obsoletas removidas | Migration aplicada | Inspecionar `information_schema.COLUMNS` de `log_notif_sms_config` | `nsc_ren_tipo`, `nsc_ren_status_max`, `nsc_condicao`, `nsc_minutos_limite` não existem mais na tabela |
| 1.4 | Colunas novas presentes | Migration aplicada | Inspecionar `information_schema.COLUMNS` de `log_notif_sms_config` | `nsc_metodo_api VARCHAR(150) NULL`, `nsc_view_consulta VARCHAR(100) NULL`, `nsc_view_dbgroup VARCHAR(30) NULL` presentes, com os tipos/tamanhos exatos da seção 5 do documento de desenvolvimento |
| 1.5 | Idempotência de `up()` | Migration já aplicada uma vez | Rodar `up()` uma 2ª vez | Nenhum erro de "coluna já existe"/"ENUM já alterado"; `adicionaColunasNovas()` e `removeColunasObsoletas()` não repetem operação sobre colunas já ajustadas (checagem via `information_schema` antes de cada `ALTER TABLE`) |
| 1.6 | Reversão por `down()` | Migration aplicada (`up()` executado) | Rodar `down()` | As 4 colunas obsoletas são recriadas (estrutura, sem dados); `ENUM` volta a `('entrega','saldo_baixo') DEFAULT 'entrega'`; as 3 colunas novas deste ciclo são removidas. Confirmar explicitamente que as regras `entrega` excluídas por `up()` **não são recuperadas** — reversão é apenas estrutural |
| 1.7 | Ordem de operações não quebra com linha `entrega` presente | Ao menos 1 regra com `nsc_tipo_regra='entrega'` cadastrada, migration ainda não aplicada | Rodar `up()` do zero, observando a sequência: (1) adiciona colunas novas → (2) `DELETE` das regras `entrega` → (3) `MODIFY ENUM` → (4) `DROP COLUMN` das obsoletas | Nenhum erro de `MODIFY ENUM` por valor incompatível — a exclusão das linhas `entrega` (passo 2) sempre roda antes da troca do `ENUM` (passo 3), confirmando a ordem obrigatória descrita na seção 4.6/5.1 do documento de desenvolvimento |
| **1.8** | **Regressão em `NotifSmsEnviadas` / linhas órfãs após exclusão de `entrega`** | Ao menos 1 registro de histórico em `log_notif_sms_enviadas` vinculado (via `nse_nsc_id`) a uma regra `nsc_tipo_regra='entrega'` pré-existente, cadastrada **antes** de rodar a migration; migration ainda não aplicada no início do caso | (1) Confirmar previamente que não há `FOREIGN KEY` entre `log_notif_sms_enviadas.nse_nsc_id` e `log_notif_sms_config.nsc_id` (só colunas `INT` simples). (2) Rodar `up()` da migration (exclui a regra `entrega` referenciada). (3) Acessar a tela `NotifSmsEnviadas` e carregar o período que inclui o histórico órfão | A tela `NotifSmsEnviadas` carrega **sem erro** mesmo com linhas órfãs em `log_notif_sms_enviadas` (regra `entrega` excluída pela migration, sem FK); `LogisNotifSmsEnviadasModel::getEnviadasPeriodo()` exibe **"Regra desconhecida/excluída"** para essas linhas (via `LEFT JOIN`/`COALESCE(..., 'Regra desconhecida/excluída')`), sem nenhuma alteração de código necessária ali — critério de aceite explícito da seção 12 do dev doc ("`NotifSmsEnviadas` continua funcionando sem alteração de código") |

## 2. Grupo 2 — CRUD `NotifSmsConfig`

| ID | Situação/Objetivo | Pré-condição | Passos | Dados de entrada | Resultado esperado |
|---|---|---|---|---|---|
| 2.1 | Regressão zero em `saldo_baixo` | Schema migrado; login com permissão `A`/`E` | Criar e depois editar uma regra `nsc_tipo_regra='saldo_baixo'` | Regra só com `nsc_saldo_minimo` + campos comuns | Comportamento idêntico ao já existente antes deste ciclo — nenhuma diferença de campo obrigatório, gravação ou toggle em relação ao ciclo anterior (`docs/desenvolvimento/notificacoes-sms-dev.md`) |
| 2.2 | Criação de regra tipo `api`, com toggle de div | Schema migrado; login com permissão `A` | Abrir `add()`, selecionar `nsc_tipo_regra='api'`, confirmar toggle de `#divApi` (visível) / `#divSaldo`,`#divConsulta` (`d-none`), escolher um método em `nsc_metodo_api`, salvar | Regra tipo `api` completa | Registro gravado com `nsc_metodo_api` preenchido; `#divApi` visível no momento do preenchimento, demais grupos ocultos |
| 2.3 | Criação de regra tipo `consulta`, com toggle de div | Schema migrado; login com permissão `A`; ao menos 1 view `vw_sms_*` de teste disponível (ver Grupo 5) | Abrir `add()`, selecionar `nsc_tipo_regra='consulta'`, confirmar toggle de `#divConsulta` (visível) / `#divSaldo`,`#divApi` (`d-none`), escolher uma view no select composto, salvar | Regra tipo `consulta` completa | Registro gravado com `nsc_view_consulta`/`nsc_view_dbgroup` corretos, obtidos do split do valor composto `dbGroup|view` — nunca do valor bruto do POST |
| 2.4 | Toggle no `onchange` de `add()` | Tela `add()` aberta | Alternar o select `nsc_tipo_regra` entre os 3 valores (`saldo_baixo`/`api`/`consulta`) | — | `alternaCamposTipoRegra(this)` disparada a cada troca; exatamente um dos 3 wrappers (`#divSaldo`/`#divApi`/`#divConsulta`) fica visível por vez; `mudaObrigatorioElemDiv()` ajusta a obrigatoriedade do grupo correspondente |
| 2.5 | Toggle no carregamento de `edit()` | Regras existentes de cada um dos 3 tipos (`saldo_baixo`, `api`, `consulta`) | Abrir `edit($id)` para cada uma das 3 regras | — | O wrapper correspondente ao `nsc_tipo_regra` salvo já nasce visível, os outros dois com `d-none`, refletindo o estado salvo sem depender de interação do usuário |
| 2.6 | `store()` rejeita `nsc_metodo_api` inválido | Schema migrado; login com permissão `A` | Submeter `store()` com `nsc_tipo_regra='api'` e `nsc_metodo_api` que não existe em `SmsApiConsumer::metodosDisponiveis()` (ex.: valor manipulado via requisição direta) | `nsc_metodo_api='metodoInexistente'` | `store()` retorna `erro=true`, mensagem "Método de API selecionado não é mais válido.", nenhum registro gravado |
| 2.7 | `store()` rejeita `nsc_view_consulta` inválida | Schema migrado; login com permissão `A` | Submeter `store()` com `nsc_tipo_regra='consulta'` e valor composto de view que não existe em `getViewsPorPrefixo('vw_sms_')` (ex.: `dbGroup|view_inexistente`) | `nsc_view_consulta='dbLogistica|vw_sms_falsa'` | `store()` retorna `erro=true`, mensagem "View de consulta selecionada não é mais válida.", nenhum registro gravado |
| 2.8 | `store()` rejeita adulteração de `nsc_view_dbgroup` | Schema migrado; login com permissão `A`; view de teste válida cadastrada em um `dbGroup` conhecido | Submeter `store()` com `nsc_tipo_regra='consulta'`, view válida mas `dbGroup` trocado por outro não correspondente (ex.: view existe só em `dbLogistica`, mas o valor composto informa `default|view`) | Valor composto com `dbGroup` incorreto para a view | Revalidação via `getViewsPorPrefixo()` não encontra o par `(dbGroup, view)` exato — `store()` retorna `erro=true`, nenhum registro gravado com `nsc_view_dbgroup` adulterado |
| 2.9 | Validação condicional por tipo via `MyValidation` | Nenhuma (não depende do schema migrado — testa a regra de validação isoladamente) | Submeter `store()` para cada tipo, omitindo o campo obrigatório do respectivo grupo: `nsc_saldo_minimo` vazio com `nsc_tipo_regra='saldo_baixo'`; `nsc_metodo_api` vazio com `nsc_tipo_regra='api'`; `nsc_view_consulta` vazio com `nsc_tipo_regra='consulta'` | 3 submissões, uma por tipo | `obrigatorioSeTipoRegraSaldo`/`obrigatorioSeTipoRegraApi`/`obrigatorioSeTipoRegraConsulta` bloqueiam `store()` em cada caso correspondente; campos dos outros 2 grupos não são exigidos |
| 2.10 | `delete()` bloqueado com histórico | Schema migrado; regra com histórico associado em `log_notif_sms_enviadas`; permissão `X` | Acionar `delete($id)` | — | Exclusão bloqueada (comportamento herdado do ciclo anterior, sem alteração neste ciclo) |
| 2.11 | `ativinativ()` permitido com histórico | Schema migrado; regra com histórico associado em `log_notif_sms_enviadas`; permissão `E` | Acionar `ativinativ($id, 'I')` | — | `nsc_ativo` gravado como `'I'` com sucesso — inativação não é bloqueada por existência de histórico |
| 2.12 | Permissão CAEXN fail-closed | Login com perfil sem `cfg_perfil_item` cadastrado para `NotifSmsConfig` | Tentar acessar diretamente as rotas (`index`/`lista`/`add`/`edit`/`delete`/`ativinativ`) | — | Acesso bloqueado em todos os métodos, resolvendo para `vw_semacesso`, conforme `rascunho-helpers-php.md` |
| 2.13 | Permissão CAEXN por granularidade de letra | Perfil de teste configurável, uma letra de cada vez | Configurar o perfil só com `C`, depois só com `A`, `E`, `X`, `N`, testando o método correspondente a cada letra | — | Cada operação só é permitida quando a letra correspondente está presente no perfil; demais operações bloqueadas |
| 2.14 | Unicidade de `nsc_nome` | Regra existente com `nsc_nome='Regra X'` | (a) tentar criar nova regra com `nsc_nome='Regra X'`; (b) editar a própria "Regra X" mantendo o mesmo nome, alterando outro campo | (a) nome duplicado; (b) mesmo nome, edição normal | (a) bloqueado por `verificaUnico()`; (b) permitido normalmente, sem falso-positivo contra o próprio registro |

## 3. Grupo 3 — `php spark notifsms:verificar`

| ID | Situação/Objetivo | Pré-condição | Passos | Resultado esperado |
|---|---|---|---|---|
| 3.1 | `saldo_baixo` sem regressão | Schema migrado; regra `saldo_baixo` ativa configurada para disparar | Rodar `php spark notifsms:verificar` | `processarRegraSaldo()` executa exatamente como no ciclo anterior (`notificacoes-sms-servico-envio-dev.md`, seção 7.1), sem alteração de comportamento |
| 3.2 | `consulta` ponta a ponta | Schema migrado; regra `consulta` ativa apontando para `vw_sms_teste` (Grupo 5); massa de dados na view | Rodar o comando | `SELECT` executado via Query Builder (`db_connect($dbGroup)->table($view)->get(...)`), nunca SQL concatenado; respeita `SMS_CONSULTA_LIMITE_LINHAS = 100`; deduplicação por `'CONSULTA:' . id` funcionando (2ª execução não reenvia os mesmos itens) |
| 3.3 | `api` com método de teste local | Schema migrado; regra `api` ativa apontando para um método de teste/stub em `SmsApiConsumer` (Grupo 5) | Rodar o comando | Método chamado sem argumentos (usa defaults internos); SMS disparado por objeto retornado; deduplicação por `'API:' . id` funcionando |
| 3.4 | `api` com retorno vazio | Schema migrado; regra `api` ativa apontando para método de teste que retorna `[]` | Rodar o comando | Nenhum SMS disparado; nenhum erro; processamento das demais regras continua normalmente |
| 3.5 | `api` com retorno não-lista | Schema migrado; regra `api` ativa apontando para método de teste que retorna array associativo (não lista) ou `null` | Rodar o comando | `processarRegraApi()` descarta o retorno sem disparar SMS (checagem `is_array()`/lista/não-vazio); nenhum erro fatal |
| 3.6 | Revalidação em tempo de execução — método removido | Schema migrado; regra `api` salva apontando para um método que existia no cadastro mas foi removido de `SmsApiConsumer` (ou renomeado) antes da execução do comando | Rodar o comando | `processarRegraApi()` detecta que o método não está mais em `metodosDisponiveis()`, loga `warning`, não interrompe o processamento das demais regras |
| 3.7 | Revalidação em tempo de execução — view removida | Schema migrado; regra `consulta` salva apontando para uma view que foi removida/renomeada no banco antes da execução do comando | Rodar o comando | `processarRegraConsulta()` detecta que o par `(dbGroup, view)` não está mais em `getViewsPorPrefixo('vw_sms_')`, loga `warning`, não interrompe o processamento das demais regras |
| 3.8 | Placeholder sem correspondência permanece literal | Schema migrado; regra `api` ou `consulta` com template contendo `[campo_inexistente]`, objeto retornado sem essa chave | Rodar o comando e inspecionar a mensagem enviada (via provider de teste/log) | `[campo_inexistente]` permanece literal na mensagem final, não vira string vazia — comportamento natural do `strtr()`, documentado como intencional |
| 3.9 | Placeholder aninhado ignorado | Schema migrado; regra `api` ou `consulta` com objeto retornado contendo uma chave de 1º nível cujo valor é array/objeto aninhado, referenciada no template (ex.: `[endereco]` onde `endereco` é um sub-objeto) | Rodar o comando e inspecionar a mensagem enviada | `substituiPlaceholders()` ignora a chave aninhada (só considera `is_array()`/`is_object()` como não substituível) — placeholder permanece literal, sem tentar serializar o sub-objeto |
| 3.10 | `nsc_tipo_regra` desconhecido não interrompe processamento | Schema migrado; uma regra com `nsc_tipo_regra` manipulado diretamente no banco para um valor fora de `saldo_baixo/api/consulta` (fora do CRUD), demais regras válidas ativas | Rodar o comando | Regra com tipo desconhecido gera apenas `warning` de log ("tipo de regra desconhecido"); as demais regras continuam sendo processadas normalmente |

**Caso adicional (não numerado no grupo, tratado à parte):** uma exceção lançada durante o processamento de 1 regra (ex.: falha de conexão com a view, exceção do provider de SMS) não deve interromper o processamento das demais regras — o `catch (\Throwable $e)` do laço principal em `run()` deve logar o erro (`logger->error`/`CLI::error`) e seguir para a próxima regra. Testar forçando uma exceção controlada em uma regra e confirmando que as regras subsequentes ainda são processadas.

`SmsApiConsumer::buscarRenovacoesPendentes()` está marcado como **"não testável de ponta a ponta nesta rodada"** — o endpoint externo `/renovacoes/pendentes` ainda não existe no repositório do Logística antigo (pendência confirmada pelo Douglas, seção 3 item 4 e seção 10 do documento de desenvolvimento). Isso não bloqueia o fechamento deste ciclo.

## 4. Grupo 4 — Testes unitários `SmsRegraExecutor` / `SmsApiConsumer`

Exigido explicitamente pelo critério de pronto da seção 12 do documento de desenvolvimento aprovado ("`SmsRegraExecutor`/`SmsApiConsumer` implementados exatamente conforme especificado ... com `metodosDisponiveis()` reflectindo corretamente"; "`SmsRegraExecutor` coberto por teste unitário isolado") — **não é opcional**, independe do schema estar migrado (testa as classes isoladamente, com dependências mockadas).

| ID | Situação/Objetivo | Passos | Resultado esperado |
|---|---|---|---|
| 4.1 | `processar()` — fluxo completo de sucesso | Chamar `processar()` com uma lista de objetos válidos, `SmsService`/`LogisNotifSmsEnviadasModel` mockados (nenhum já enviado) | Retorna a contagem correta de itens processados; `SmsService::enviar()` chamado uma vez por telefone por objeto; `LogisNotifSmsEnviadasModel::registrar()` chamado uma vez por objeto |
| 4.2 | `extrairIdDedup()` — com `id` escalar presente | Chamar (via reflection, método privado) com objeto contendo `['id' => 123, ...]` | Retorna `'123'` (cast para string do valor de `id`), sem cair no `md5()` |
| 4.3 | `extrairIdDedup()` — sem `id` | Chamar com objeto sem chave `id` | Retorna `md5(json_encode($objeto))` |
| 4.4 | `substituiPlaceholders()` — só 1º nível | Chamar com template contendo `[campo1]` e chave aninhada, objeto com `campo1` escalar e uma chave cujo valor é array | Placeholder de `campo1` substituído corretamente; chave aninhada não é convertida em placeholder utilizável (ignorada, `is_array()`) |
| 4.5 | `substituiPlaceholders()` — placeholder sem correspondência | Chamar com template contendo `[campo_ausente]`, objeto sem essa chave | `[campo_ausente]` permanece literal na string retornada |
| 4.6 | Item já enviado é pulado | Chamar `processar()` com um objeto cuja chave de dedup já retorna `true` em `jaEnviado()` (mock) | `SmsService::enviar()` **não** é chamado para esse item; contagem de processados não inclui esse item |
| 4.7 | Item não-array é ignorado | Chamar `processar()` com uma lista contendo ao menos 1 item que não é `array` (ex.: string, `null`, escalar) misturado a itens válidos | O item não-array é ignorado silenciosamente (`continue`), sem lançar exceção; os demais itens válidos são processados normalmente |
| 4.8 | Split correto de telefones CSV | Chamar `processar()` com `$telefonesCsv = "5548999998888, 5548911112222,5548922223333"` (com espaço variável) | `SmsService::enviar()` chamado 1 vez por telefone, 3 vezes no total, cada telefone já sem espaços em branco (`trim()` aplicado por item do `explode(',', ...)`) |
| **4.9** | **`SmsApiConsumer::metodosDisponiveis()` filtra corretamente (unitário, não bloqueado pela migration)** | Criar uma fixture/subclasse de teste (ou classe de teste equivalente que reproduza a mesma estrutura de reflection usada por `SmsApiConsumer::metodosDisponiveis()`) contendo, no mínimo: (a) 1 método público estático; (b) 1 método público com parâmetro obrigatório; (c) 1 método herdado de classe-pai (não declarado na própria classe); (d) 1+ método(s) público(s) sem parâmetro obrigatório, declarado(s) na própria classe. Chamar `metodosDisponiveis()` e inspecionar o array retornado | O array retornado contém **somente** os métodos públicos, não-estáticos, não-construtor, declarados na própria classe (`getDeclaringClass()->getName() === self::class`), sem parâmetro obrigatório (`getNumberOfRequiredParameters() === 0`) — ou seja: o método estático (a), o método com parâmetro obrigatório (b) e o método herdado (c) **não aparecem** no retorno; apenas (d) aparece, com chave e valor iguais ao nome do método. **Este caso não depende da migration — pode ser executado imediatamente, sem esperar confirmação do Douglas.** |

## 5. Grupo 5 — Pré-requisitos de ambiente

| Item | Descrição | Status |
|---|---|---|
| 5.1 | Rodar a migration `2026-07-22-000001_LogisticaNotifSmsTiposApiConsulta.php` em ambiente de dev | **Aguardando confirmação explícita do Douglas** — bloqueante para os Grupos 1 (inclusive o caso 1.8), 3 e a maior parte do Grupo 2 |
| 5.2 | Criar view `vw_sms_teste` de teste em algum `dbGroup` conhecido, com massa de dados mínima para validar o tipo `consulta` de ponta a ponta (casos 2.3, 3.2) | Pendente de criação |
| 5.3 | Confirmar `dbLogistica` incluído em `$dbGroupsConhecidos` (`ConfigDicDadosModel`), conforme seção 4.2 do documento de desenvolvimento | Pendente de conferência após codificação |
| 5.4 | Criar método de teste local em `SmsApiConsumer` (ou stub isolado equivalente), sem impactar `buscarRenovacoesPendentes()`, para viabilizar os casos 2.2, 3.3, 3.4, 3.5, 3.6 | Pendente de criação — deve ser público, sem parâmetro obrigatório, para aparecer em `metodosDisponiveis()` |
| 5.5 | Confirmar registro de tela/permissões CAEXN (`ConfigTelaModel`/`ConfigPerfilItemModel`) para os perfis de teste usados nos casos 2.12/2.13 | Pendente de conferência — tela `NotifSmsConfig` já existe de ciclo anterior, mas confirmar que as permissões cobrem os perfis de teste desta rodada |
| **5.6** | **Cadastrar ao menos 1 registro de histórico em `log_notif_sms_enviadas` vinculado a uma regra `entrega` pré-existente, antes de rodar a migration, para viabilizar o caso 1.8** | Pendente de criação — pré-condição específica do caso 1.8, executar antes do item 5.1 |

## Pendências antes da execução

- **Migration não executada em dev.** A migration `2026-07-22-000001_LogisticaNotifSmsTiposApiConsulta.php` exclui definitivamente as regras `nsc_tipo_regra = 'entrega'` já cadastradas — conforme a regra do projeto de nunca rodar migration sem confirmação explícita do usuário, **é necessária a confirmação do Douglas antes de executar** `up()` **em dev**. Enquanto isso não ocorrer, os casos do Grupo 1 (incluindo o 1.8), os casos do Grupo 3 e a maior parte dos casos do Grupo 2 (exceto o caso 2.9, que testa a validação condicional de forma isolada, sem depender do schema novo) permanecem bloqueados para execução. **O caso 4.9 é exceção: unitário e isolado, pode ser executado desde já, sem depender da migration.**
- Antes de rodar a migration para o caso 1.8, garantir que existe ao menos 1 registro de histórico em `log_notif_sms_enviadas` vinculado a uma regra `entrega` pré-existente (item 5.6) — senão o caso perde a cobertura de "linha órfã".
- `buscarRenovacoesPendentes()` **não testável de ponta a ponta nesta rodada.** O endpoint externo `GET /renovacoes/pendentes` ainda não foi implementado no repositório do Logística antigo (pendência externa, confirmada pelo Douglas — seção 3, item 4 e seção 10 do documento de desenvolvimento). O método fica pronto e selecionável na tela desde já, mas seu teste de ponta a ponta depende da existência do endpoint do outro lado. **Esta pendência não bloqueia o fechamento do ciclo**, conforme critério de pronto explícito na seção 12 do documento de desenvolvimento aprovado.
- **Pré-requisitos de ambiente do Grupo 5** (view `vw_sms_teste`, método de teste local em `SmsApiConsumer`, confirmação de `dbLogistica` em `$dbGroupsConhecidos`, permissões CAEXN dos perfis de teste, histórico órfão pré-migration para o caso 1.8) precisam estar concluídos antes da execução dos casos que dependem deles (ver referência cruzada em cada item do Grupo 5).

## Resumo dos 3 ajustes aplicados desta rodada de revisão do `byarq`

1. Caso **1.8** adicionado ao Grupo 1 — cobre a regressão em `NotifSmsEnviadas`/linhas órfãs em `log_notif_sms_enviadas` após o `DELETE` definitivo das regras `entrega` (sem FK), validando o `LEFT JOIN`/`COALESCE(..., 'Regra desconhecida/excluída')` já existente em `LogisNotifSmsEnviadasModel::getEnviadasPeriodo()`.
2. Caso **4.9** adicionado ao Grupo 4 — teste unitário isolado de `SmsApiConsumer::metodosDisponiveis()`, cobrindo exclusão de método estático, método herdado e método com parâmetro obrigatório; explicitamente marcado como não bloqueado pela migration.
3. Critério de aceite do caso **1.1** corrigido para a string completa e exata `"{$qtd} regra(s) do tipo 'entrega' excluída(s) definitivamente (tipo descontinuado, confirmado pelo Douglas)."`, confirmada por leitura direta da migration.
